def getElement(arr, n):
    if n == 0 or n == 1:
        print(-1, -1)
        return

    arr.sort()
    seclarge = arr[n - 2]
    print("Second largest element is:", seclarge)

if __name__ == "__main__":
    arr = [1, 2, 3, 4, 6, 5]
    n = len(arr)
    getElement(arr, n)
